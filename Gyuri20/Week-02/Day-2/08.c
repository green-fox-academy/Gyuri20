#include <stdio.h>

int main() {
	int h1 = 350;
	int h2 = 200;

	if ( h1 < (h2 << 1)){

        printf("I tell you, h2 is bigger than h1");

        }
	// tell if the double of h2 is bigger than h1
	return 0;
}
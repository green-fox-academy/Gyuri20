#include <stdio.h>

int main() {
    int n,
    times = 20;

   /* for loops execution */
   for( n = 0; n <= times; n = n + 2 )
   {
      printf("numbers till 20: %d\n", n);
   }
	// print the even numbers till 20
	return 0;
}
